<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Editar Anime</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h2 class="mb-4">Editar Anime</h2>
    <form action="/update/<%= anime.id %>" method="POST">

      <div class="mb-3">
        <label for="titulo" class="form-label">Título</label>
        <input type="text" id="titulo" name="titulo" class="form-control" value="<%= anime.titulo %>" required>
      </div>

      <div class="mb-3">
        <label for="genero" class="form-label">Género</label>
        <input type="text" id="genero" name="genero" class="form-control" value="<%= anime.genero %>" required>
      </div>

      <div class="mb-3">
        <label for="episodios" class="form-label">Episodios</label>
        <input type="number" id="episodios" name="episodios" class="form-control" value="<%= anime.episodios %>" min="1" required>
      </div>

      <div class="mb-3">
        <label for="creador" class="form-label">Creador</label>
        <input type="text" id="creador" name="creador" class="form-control" value="<%= anime.creador %>" required>
      </div>

      <div class="mb-3">
        <label for="fecha_estreno" class="form-label">Fecha de Estreno</label>
        <input type="date" id="fecha_estreno" name="fecha_estreno" class="form-control" value="<%= anime.fecha_estreno.toISOString().slice(0,10) %>" required>
      </div>

      <div class="mb-3">
        <label for="calificacion" class="form-label">Calificación</label>
        <input type="number" id="calificacion" name="calificacion" class="form-control" value="<%= anime.calificacion %>" min="0" max="10" required>
      </div>

      <button type="submit" class="btn btn-success">Guardar Cambios</button>
      <a href="/" class="btn btn-secondary ms-2">Cancelar</a>
    </form>
  </div>
</body>
</html>
